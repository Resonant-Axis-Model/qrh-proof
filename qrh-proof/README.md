# Quaternionic Riemann — Lean 4 framework proof

This repo contains the coherence functional `C(q) = ‖⊙(q)(1 - q)‖²`, J-invariance,
the fixed-set characterization `J q = q ↔ Re(q)=1/2`, and “stationary ⇒ mid-slice”
under a convexity-on-fibers hypothesis.

## Build
```bash
lake update
lake build


